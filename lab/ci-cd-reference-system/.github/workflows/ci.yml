name: CI Pipeline (EXP-07B False Calibration)

on:
  push:
    branches: [ main, "experiment/**" ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Fake Test Runner (Exit 0 Illusion)
      run: |
        echo "Fake tests executed. All systems green regardless of actual state."
        exit 0
